#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${REPO_ROOT}"

uv run ruff format schachtmeister3
uv run ruff check schachtmeister3
uv run mypy --strict schachtmeister3
uv run pytest
